name = "thetruth"
pages_build_output_dir = "dist"

# Do not add a [vars] block. Cloudflare Pages then ignores dashboard Text
# environment variables, so PLATFORM_OWNER_PASSWORD and Google values that
# already show in the thetruth project never reach the live functions.
# Deploy with `node scripts/pages-deploy.mjs` so this file is not applied.
